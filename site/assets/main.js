const form = document.querySelector(".signup-form");

form?.addEventListener("submit", (event) => {
  event.preventDefault();
  alert("Дякуємо! На наступному етапі тут буде підключена форма заявки.");
});

